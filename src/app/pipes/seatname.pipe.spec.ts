/* tslint:disable:no-unused-variable */
import { SeatnamePipe } from './seatname.pipe';

describe('Pipe: Seatnamee', () => {
  it('create an instance', () => {
    let pipe = new SeatnamePipe();
    expect(pipe).toBeTruthy();
  });
});
